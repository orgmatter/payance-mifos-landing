import React from 'react';
import { LearnBody } from '../../components/body';

export default function Learn(props) {

    const {  } = props;

    return (
        <div className="learn-cover">
            <LearnBody />
        </div>
    )
}